{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "NODE_OPTIONS": "--no-deprecation --max-old-space-size=8000"
    }
  },
  "installCommand": "npm install",
  "buildCommand": "npm run generate:importmap && npm run build"
}
